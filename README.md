# git_sync
Script for unidirectional sync of selective git branches.
Within certain worflows it is needed to sync some of the branches from one repo to another and some other branches back. 
The typical scenario is syncing all feature branches from working repo to the main one and integraion branches back.
## Usage
